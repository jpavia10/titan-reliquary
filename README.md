# Titan Reliquary (live viewer)

Public GitHub Pages build of the portable vault **app only**.

- Live: https://jpavia10.github.io/titan-reliquary/
- Source of truth stays on Joseph's machine (`/home/box/collection`). This repo never holds LEDGER/markdown/JSON vault files.
- Republish (Desktop + Pages + Drive together): `bash /home/box/collection/gui/publish_all.sh`
- Contents: `index.html`, `version.json`, `photos/{CONT}/{ISO}/{SER}_{year}_{denomSlug}_{obv|rev}.jpg` (existing photos only).
